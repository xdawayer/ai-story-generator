# LLM provider — Azure OpenAI (GPT). Copy values from gengrowth-agents/.env.local
# into your own .env.local (which is gitignored and never committed).
AZURE_OPENAI_API_KEY=
AZURE_OPENAI_ENDPOINT=
AZURE_OPENAI_DEPLOYMENT=
OPENAI_API_VERSION=2025-03-01-preview
